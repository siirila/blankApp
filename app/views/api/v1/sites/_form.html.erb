<%= form_for(@api_v1_site) do |f| %>
  <% if @api_v1_site.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@api_v1_site.errors.count, "error") %> prohibited this api_v1_site from being saved:</h2>

      <ul>
      <% @api_v1_site.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :url %><br>
    <%= f.text_field :url %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
